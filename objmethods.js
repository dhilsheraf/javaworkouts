const object = {
    name : "shalik",
    age : 20,
    house : "kottakkal",
    domain : "Data science",
}

const key = Object.keys(object)
console.log(key);

console.log(Object.entries(object));

let age = 18;
let canVote = age <= 18 ? 'Yes, you can vote' : 'No yo cannot vote'

console.log(canVote);


